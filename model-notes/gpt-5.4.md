# GPT-5.4

Best default Codex model for difficult implementation, deep debugging, or repo-aware orchestration.

Use when:

- the packet is high-impact or technically dense
- verification will be expensive if the first pass is weak
- you need strong execution realism

Watch for:

- unnecessary cost on small packets
- overuse when `gpt-5.4-mini` would be enough
