# Example Workspace AGENTS

This example workspace uses RelayKit as a lightweight overlay.

## Minimum Rule

- use the workspace profile for defaults
- use the project profile only for real overrides
- keep the repo-local `AGENTS.md` small
- do not add extra coordination files unless parallel work actually needs them
